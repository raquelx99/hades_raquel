using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bau : MonoBehaviour
{
    Animator tampaBau;

    // Start is called before the first frame update
    void Start()
    {
        tampaBau = GetComponent<Animator>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
